# HappyMac 半年开发路线（2026-09 → 2027-02，业余节奏）

> 节奏假设：每周 1-2 个晚间 + 隔周一个半天 ≈ **8-12 有效小时/周**，半年约 250-300 小时。
> 排期原则：**宁小勿大**——每周只排一个"最小可完成单元"；每月留最后一个周末做缓冲，不排任务。
> 铁律：感知参数改动必须走 `simulator → eval_replay → 上机` 三步（`simulator/` 离线闭环）；新数据进 `sessions_v2/` 并当场过质量门。

---

## 总览：三个月度主线 + 两条并行支线

```
主线（串行依赖）：外壳定稿 → V2 采集 → 重训裁决 → 部署链 → 论文
支线A（随时可做，无硬件依赖）：动画体验 / 模拟器 / 文档
支线B（跟采集顺路）：Paper 1 素材（干扰AB、距离曲线）
```

| 月份 | 主题 | 里程碑（月底验收物） |
|---|---|---|
| M1 09月 | 收尾 v0 + 硬件回归 | 板子回归 48h 内完成烧录对拍；外壳 v1 打印件在手 |
| M2 10月 | 装壳定稿 + V2 首采 | V2 第一轮数据过质量门 |
| M3 11月 | V2 采集完成 + 转头裁决 | 最终模型定稿；Paper 1 素材采齐 |
| M4 12月 | 部署链 + Paper 1 初稿 | INT8 模型上机；Paper 1 draft |
| M5 01月 | 体验打磨 + 送人版 | 送人可用版 ×1 |
| M6 02月 | Paper 2 写作 + 复盘 | Paper 1/2 投稿就绪 |

---

## M1（9月）：收尾 v0 + 硬件回归

板子不在手边的时段，把所有"不需要硬件"的事排满。

| 周 | 任务 | 产出 |
|---|---|---|
| W1 | 采集固件三处改动：2410 灵敏度 40/40（可配宏）、3 目标 CSV、FW_VERSION 入 meta | `collection_v2` 固件分支，编译通过 |
| W2 | 外壳 CAD 初稿 + 3D 打印 v1；S3 竖直朝向工装 | 打印件到手 |
| W3 | **板子回归**：烧录当前固件 → 真机对拍（`EV/STATE/ANIM` 串口日志 vs 模拟器逐行比对） | 对拍记录入库 |
| W4 | 眼神惯性：方向翻转滞回 + 二阶平滑（治"太着急"）；模拟器同步 | eval 无回退，真机观感确认 |

验收问题：真机静坐 10 分钟，误笑 0 次且表情连续？睡前链真机完整播一遍？

## M2（10月）：装壳定稿 + V2 首采

| 周 | 任务 | 产出 |
|---|---|---|
| W1-2 | 外壳迭代（v2/v3 修装配问题）；装壳前后各录一场对照（量化壳的影响，兼作 Paper 1 素材） | 装壳定稿 + 对照数据 |
| W3 | 装壳后阈值现场校准（用 eval 流程重扫 MOTION/confirm） | 校准参数入库 |
| W4 | **V2 第一轮采集**（按 `training/DATA_COLLECTION_V2.md` SOP：基线+规则+自由） | R1 数据过 9 项质量门 |

注意：外壳迭代若拖延，**用夹具临时固定几何先行采集**（规范允许，几何登记照写）——别让外壳阻塞数据线。

## M3（11月）：V2 采集完成 + 转头裁决（本项目半年内最重要的决策点）

| 周 | 任务 | 产出 |
|---|---|---|
| W1 | V2 第二轮采集（不同天、不同衣装） | R2 数据 |
| W2 | 重标定模拟器噪声模型 → `exp_head_separability.py` 转头裁决 | go/no-go 结论 |
| W3 | **go**：加 HEAD 类重训（export_tree → eval → 固件换树）；**no-go**：维持"小幅归静"，直接做 4 类重训 | 新树 + eval 报告 |
| W4 | 误笑率 <10% 验收；双雷达干扰 AB 测试（3 轮，Paper 1 素材） | 验收记录 |

裁决标准（V2 规范 §7 写死的）：HEAD 召回 >75% 且打字误报 <10%。

## M4（12月）：部署链 + Paper 1 初稿

| 周 | 任务 | 产出 |
|---|---|---|
| W1-2 | RF → MLP 蒸馏 → INT8 量化 → C3 上机（`train.py` 管线已有）；MLP vs 树的 eval 对比 | INT8 上机跑通 + 选型报告 |
| W3 | **Paper 1 初稿**：5 距离 × 4 行为曲线 + 干扰 AB + 8 月表征报告整合 | draft v0 |
| W4 | 缓冲/补漏（12 月假日系数，别排满） | — |

学术口径：Paper 1 = 物理表征（"40 元双雷达在桌面尺度的能力边界"），素材 M2/M3 已顺路采齐。

## M5（1月）：体验打磨 + 送人版

| 周 | 任务 | 产出 |
|---|---|---|
| W1 | 作息钟 + 深夜参数调制（"困但撑着陪你"） | 昼夜行为差异可演示 |
| W2 | **惊醒假装没睡**（深夜 SLEEP + Y 趋势触发，事件系统插槽已留） | 招牌动画落地 |
| W3 | 发呆涣散态；电源优化实测（雷达占空比策略，803040 续航目标翻倍） | 送人版定稿 |
| W4 | 新模型装壳校准第二轮；送人版 ×1 组装 | 可送出 |

## M6（2月）：Paper 2 写作 + 半年复盘

| 周 | 任务 | 产出 |
|---|---|---|
| W1-2 | **Paper 2 初稿**：跨模态蒸馏管线 + V2 数据 + 消融（X/Es/Y 通道、协议vs自然、误笑率作为产品指标的故事） | draft v0 |
| W3 | 全量回归 + 模拟器最终校准；仪器模式整理（导师 demo 用） | 冻结版 |
| W4 | 半年复盘：投稿目标选定（期刊/workshop）；下一半年方向 = Paper 3（动画层用户研究） | 复盘文档 |

---

## 依赖图与关键路径

```
外壳定稿 ──→ V2 采集(R1+R2) ──→ 重训裁决 ──→ 部署链 ──→ Paper 2
   │              │
   │              └──(顺路)──→ Paper 1 素材 ──→ Paper 1 初稿（M4）
   └─(可被夹具绕过，不阻塞数据线)

动画/电源支线（M1/M5）与主线并行，不抢资源。
```

**关键路径上的唯一硬前置是外壳**——所以 M1 就开工 CAD，M2 留了"夹具绕过"的逃生门。

## 风险与预案

| 风险 | 预案 |
|---|---|
| 外壳迭代拖过长 | 夹具临时固定先行采集（几何登记照写），外壳与数据解耦 |
| 转头仍不可分 | 按既定决策搁置产品化，主线不受影响（V2 数据照样服务 4 类重训） |
| MLP 不如决策树 | 树留作产品模型，MLP 结果进论文——两条腿都不浪费 |
| 假日/学业挤占 | 每月 W4 缓冲 + 月度里程碑只设 1 个硬验收物；砍顺序：Paper 1 < 电源 < 动画 < 数据 < 部署 |
| 板子长期不回 | M1 的 W1-W2 任务与模拟器工作可支撑约一个月；对拍推迟到 M2 |
| 调敏后误报上升 | `eval_replay --sweep` 重扫参数（规范已写） |

## 本周就能做的三件事（不等板子）

1. 采集固件三处改动（`collection_v2` 分支）
2. 外壳 CAD 初稿（量一下 803040 + 双雷达 + OLED 的堆叠高度）
3. 眼神惯性参数（模拟器先行）

---

*依赖既有资产：`simulator/` 离线闭环、`training/DATA_COLLECTION_V2.md` 采集规范、事件系统插槽、`train.py` 蒸馏管线、8 月表征报告 docx。本路线由 2026-08-30/09-06 的工作推导。*
